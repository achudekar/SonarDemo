version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto11
    commands:
      - apt update && apt install -y postgresql-client
  pre_build:
    commands:
      - mvn verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=achudekar_SonarDemo > log.txt
